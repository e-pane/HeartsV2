const rawThemeInputs = {
  buttonColor: buttonColorInput.value,
  cardBack: cardBackSelect.value,
  playerAvatarStyle: avatarSelect.value,
  cardFaceStyle: cardFaceSelect.value,
  tableLogo: logoSelect.value,
};
